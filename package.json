{
  "dependencies": {
    "bulma": "^0.9.3",
    "bulma-calendar": "^6.1.15",
    "bulma-o-steps": "^1.1.0",
    "create-elm-app": "^5.22.0",
    "dotenv": "^16.0.3",
    "serve": "^14.0.1"
  },
  "scripts": {
    "db": "cd DB && json-server --watch db.json --port 3004",
    "build": "npm i && elm-app build",
    "css-build": "sass src/styles.scss src/styles.css", 
    "css-watch": "npm run css-build -- --watch"
  },
  "devDependencies": {
    "sass": "^1.55.0"
  }
}
